providers:
  embedder:
    provider_id: embedder.fake_alt
    params:
      dim: 12
  llm:
    provider_id: llm.fake
    params:
      name: fake-llm-alt
  vector_index:
    provider_id: vector.in_memory
  reranker:
    provider_id: reranker.noop

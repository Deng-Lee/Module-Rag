[pytest]
testpaths = tests
addopts = -q
markers =
    unit: fast tests that run by default (no external services)
    integration: tests that may touch real storage/services; not run by default
    e2e: end-to-end tests; not run by default

